# 飞书备份研究日志 - 20260323-0557

## 研究时间
2026-03-23 05:57 (Asia/Shanghai)

## 研究目标
找到可靠的飞书备份方案，不一定要用 Bitable

---

## 尝试 1：feishu_bitable_create_app API 调用

### 参数
```json
{
  "name": "🧪 飞书备份测试 Bitable - 20260323-0557"
}
```

### 结果
❌ **失败** - 返回 400 错误

### 错误信息
```
Request failed with status code 400
```

### 分析
- 权限检查：已确认有 `base:app:create` 权限（user 类型）
- 可能原因：
  1. API 需要 folder_token 参数（但 schema 标记为 optional）
  2. API 实现可能有 bug 或参数验证问题
  3. 需要额外的 tenant 级别权限

---

## 权限状态检查

已确认的 Bitable 相关权限（user 类型）：
- ✅ `base:app:create` - 创建应用
- ✅ `base:app:read` - 读取应用
- ✅ `base:app:update` - 更新应用
- ✅ `base:app:copy` - 复制应用
- ✅ `base:table:create` - 创建表格
- ✅ `base:table:read` - 读取表格
- ✅ `base:table:update` - 更新表格
- ✅ `base:table:delete` - 删除表格
- ✅ `base:field:create` - 创建字段
- ✅ `base:field:read` - 读取字段
- ✅ `base:field:update` - 更新字段
- ✅ `base:field:delete` - 删除字段
- ✅ `base:record:create` - 创建记录
- ✅ `base:record:retrieve` - 获取记录
- ✅ `base:record:update` - 更新记录
- ✅ `base:record:delete` - 删除记录
- ✅ `base:view:read` - 读取视图
- ✅ `base:view:write_only` - 写入视图

权限充足，但 API 调用失败。

---

## 替代方案探索

### 方案 A：云文档 + 表格块（feishu_doc create_table）✅ **推荐**

**状态：** 已验证可行

**优势：**
- 权限充足（`docx:document:create`, `docx:document:write_only`）
- 已有多次成功实践记录
- 支持结构化数据存储
- 可读取和更新

**劣势：**
- 不如 Bitable 灵活（无字段类型、视图等）
- 查询能力有限

**实施方法：**
1. 创建云文档
2. 使用 `feishu_doc create_table` 创建表格
3. 使用 `feishu_doc write_table_cells` 写入数据
4. 定期更新文档内容

---

### 方案 B：使用现有 Bitable（如果用户已有）

**状态：** 需要用户提供现有 Bitable URL

**优势：**
- 功能完整
- 无需创建新应用

**劣势：**
- 依赖用户已有资源
- 需要手动配置

**实施方法：**
1. 请用户提供现有 Bitable URL
2. 使用 `feishu_bitable_get_meta` 解析
3. 使用 `feishu_bitable_create_field` 创建字段
4. 使用 `feishu_bitable_create_record` 写入数据

---

### 方案 C：云盘文件备份（JSON 原始数据）✅ **推荐**

**状态：** 已验证可行（云盘中有大量备份文档）

**优势：**
- 简单直接
- 无需复杂 API
- 完整保留数据结构
- 易于恢复

**劣势：**
- 非结构化
- 需要额外解析

**实施方法：**
1. 将备份数据序列化为 JSON
2. 使用 `feishu_doc create` 创建文档
3. 使用 `feishu_doc write` 写入 JSON 内容
4. 可选：同时创建 Markdown 格式便于阅读

---

## 推荐备份策略

### 🏆 最佳方案：云文档表格方案

**推荐指数：** ⭐⭐⭐⭐⭐ (5/5)

**理由：**
- ✅ 权限充足（已验证）
- ✅ API 稳定（已验证完整流程）
- ✅ 实施简单
- ✅ 支持结构化数据
- ✅ 便于查看和管理

### 备份架构设计

```
飞书云盘/
├── 📋 备份索引/           # 云文档表格
│   └── 备份索引总览.docx   # 记录所有备份的元数据
├── 📄 备份内容/           # 云文档
│   ├── MEMORY-20260323.docx
│   ├── SOUL-20260323.docx
│   └── ...
└── 📊 备份统计/           # 云文档表格
    └── 备份统计报表.docx   # 备份成功率、大小趋势等
```

### 数据结构设计

**备份索引表格字段：**
| 字段 | 类型 | 说明 |
|------|------|------|
| backup_id | Text | 唯一标识 (BK+ 时间戳 + 序号) |
| backup_time | Text | 备份时间 (ISO 格式) |
| file_name | Text | 原文件名 |
| doc_token | Text | 飞书文档 token |
| doc_url | Text | 飞书文档 URL |
| file_size | Number | 文件大小 (bytes) |
| status | Text | 状态 (success/failure) |
| notes | Text | 备注 |

### 实施建议

1. **立即实施：** 云文档表格方案（已验证可行）
2. **并行实施：** JSON 原始数据备份（作为冗余）
3. **长期优化：** 如需要高级查询/视图功能，再考虑 Bitable 方案

---

## 额外权限需求

**当前方案：** 无需额外权限
- `docx:document:create` ✅
- `docx:document:write_only` ✅
- `docx:document:readonly` ✅

**Bitable 方案（备选）：** 需要排查 API 调用失败原因
- `base:app:create` ✅ (权限有，但 API 返回 400)
- 可能需要企业管理员配置

---

## 实践测试结果（20260323-0557 更新）

### 云文档表格方案完整流程验证

**测试文档：** https://feishu.cn/docx/JYFudRcP7osS4IxdEjUczm11nAj

| 步骤 | 操作 | API | 结果 |
|------|------|-----|------|
| 1 | 创建文档 | feishu_doc create | ✅ 成功 |
| 2 | 创建表格 | feishu_doc create_table | ✅ 成功 (3 行 5 列) |
| 3 | 写入数据 | feishu_doc write_table_cells | ✅ 成功 (15 个单元格) |
| 4 | 读取验证 | feishu_doc read | ✅ 成功 (可读取内容) |
| 5 | 插入行 | feishu_doc insert_table_row | ❌ 失败 (需要 block_id 参数) |

**结论：** 核心流程（创建→表格→写入）完全可行！插入行需要额外参数，但可以通过重新创建表格或一次性写入足够行数来解决。

---

## 下一步计划

- [x] 验证 feishu_doc create_table 完整流程
- [ ] 设计备份数据结构
- [ ] 实施首次完整备份
- [ ] 建立备份索引机制
- [ ] 配置定时备份任务

---

## 研究笔记

### Bitable API 创建失败原因分析

可能原因：
1. API 实现问题（参数验证过严）
2. 需要额外的企业权限配置
3. 需要先在飞书界面手动创建过一个 Bitable 才能使用 API
4. folder_token 虽然标记为 optional，但实际可能需要

### 云文档方案优势

虽然功能不如 Bitable 强大，但对于备份场景已经足够：
- ✅ 权限充足、实施简单
- ✅ 支持结构化数据（表格）
- ✅ 易于查看和分享
- ✅ API 稳定可靠

### 关键发现

**核心流程已验证可行：**
1. 创建文档 → ✅
2. 创建表格 → ✅
3. 写入数据 → ✅
4. 读取内容 → ✅

**注意事项：**
- 创建表格时需预估足够行数（insert_table_row 需要额外参数）
- 表格数据一次性写入效率最高
- 文档 URL 格式：`https://feishu.cn/docx/{doc_token}`

---

## ✅ 最终结论

**推荐方案：云文档表格备份方案**

- 可行性：⭐⭐⭐⭐⭐
- 实施难度：⭐⭐☆☆☆ (简单)
- 维护成本：⭐⭐☆☆☆ (低)
- 扩展性：⭐⭐⭐☆☆ (中等)

**下一步：** 开始实施首次完整备份，建立备份索引机制。
